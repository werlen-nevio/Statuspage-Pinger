;
; A narrow range so the firewall rule stays small. Verify what Asterisk really
; took with "rtp show settings" — on an empty or inverted range it falls back to
; 5000-31000 with only a LOG_WARNING, which the default logger does not show.
;
[general]
rtpstart=20000
rtpend=20100

[ice_host_candidates]
